/*
5. String Splitter

Write a recursive function that splits a string based on a separator (similar to String.prototype.split). Don't use JS array's split function to solve this problem.

    Input: 02/20/2020
    Output: ["02", "20", "2020"]
*/

const shredder = function(string) {
    if(string.length === 0) {
        return "Finished!"
    }
    let newString = [];
    return 
}